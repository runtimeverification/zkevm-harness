[package]
name = "mload-test"
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = "z"

[workspace]

[dependencies]
revm-interpreter = { version = "15.2.0", default-features = false }
{{ zkvm_deps }}
